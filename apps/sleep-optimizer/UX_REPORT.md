# UXレビューレポート — 睡眠サイクル計算ツール（sleep-optimizer）

**レビュー日**: 2026-05-18
**レビュアー**: UXレビュアーエージェント（ux-reviewer）
**対象ファイル**: index.html / style.css / script.js

---

## ペルソナ設定

| ID | 属性 | 利用シーン | 主な端末 |
|----|------|-----------|---------|
| A  | 20代男性会社員・残業続きで睡眠不足 | 23時過ぎに「今から寝たら何時に起きれば良いか」をスマホで検索 | iPhone（Safari） |
| B  | 30代女性・子育て中・睡眠の質改善希望 | 子どもを寝かしつけた後に「明日7時に起きるには何時に寝ればいい？」を調べる | Android Chrome |
| C  | 大学生・就活前・睡眠スケジュール最適化 | PCで面接前夜のベスト就寝時刻を調べる | MacBook Chrome |

---

## 評価サマリー

| 観点 | 評価 | 主な所見 |
|------|------|---------|
| 1. ファーストビュー（30秒テスト） | ✅ 良好 | 目的・操作方法が即座に理解できる |
| 2. 入力UX | ⚠️ 軽微な問題 | 入眠所要時間のバリデーション・説明不足 |
| 3. 計算・結果の表示 | ✅ 良好 | カード形式・おすすめバッジで視認性高い |
| 4. エラー・空状態 | ⚠️ 要改善 | バリデーションエラー表示が未実装 |
| 5. モバイル・タッチ対応 | ✅ 良好 | 44pxルール準拠・iOSズーム防止済み |
| 6. 信頼性・プライバシー | ✅ 良好 | localStorage説明あり・プライバシーポリシーリンクあり |
| 7. コンテンツ・離脱防止 | ⚠️ 軽微な問題 | 睡眠KWに沿ったFAQが未実装・アフィリエイトが睡眠と無関係 |
| 8. 広告・アフィリエイト | ⚠️ 要改善 | アフィリエイト案件（ドメイン登録）がツールと無関係で信頼感を損なう |

---

## 詳細レビュー

### 1. ファーストビュー（30秒テスト）

**評価: ✅ 良好**

- header h1「睡眠サイクル計算ツール（REM周期から最適時刻を算出）」でツール目的が一目瞭然
- subtitleに「90分のREM周期と入眠所要時間から」と計算ロジックの根拠が明示されており信頼感がある
- 2タブ構造（起床から逆算 / 就寝から順算）でユースケースを瞬時に選択できる
- デフォルト値（07:00 / 23:00）が設定済みで、初期表示から計算結果が見える

**改善提案（軽微）**:

| 場所 | ペルソナ | 問題 | 修正案 |
|------|---------|------|-------|
| index.html 33行目 h1 | ペルソナA | h1が「睡眠サイクル計算ツール」だが、SEO_REPORTの推奨h1は「睡眠時間計算ツール」。ターゲットKW「睡眠時間 計算」（月間14,800）が含まれていない | h1を`睡眠時間計算ツール — REM周期で起床・就寝時刻を逆算`に変更 |
| index.html 33-34行目 | ペルソナB | 「何時に寝ればいい？」という口語検索KW（月間8,000〜12,000）がファーストビューに全く登場しない | subtitleまたはpanel-descに「何時に寝ればいい？すぐ分かる」などの口語表現を自然に追加 |

---

### 2. 入力UX

**評価: ⚠️ 軽微な問題あり**

- ラベルとinputの紐付けは `for`/`id` で正しく実装されている
- `type="time"` でネイティブのタイムピッカーが使えるのはモバイルで直感的
- デフォルト値あり・リアルタイム計算で「ボタン押し忘れ」がない点は優秀

**問題と修正案**:

| 場所 | ペルソナ | 問題 | 修正案 |
|------|---------|------|-------|
| index.html 62行目 `input[type="number"]` | ペルソナB | 入眠所要時間の意味が分からないユーザーが多い。`min="1"` だが `max="60"` の制約の意味も不明瞭 | ラベルの後に `(平均14分)` などのヒントテキスト（`<small>`）を追加し、「自分の寝つき時間が分からない人はデフォルト14分のままでOK」と補足 |
| index.html 62行目 / script.js 249行目 | ペルソナA | 入眠所要時間に `0` や `-5` や `100` など範囲外を入力しても計算は実行される（`parseInt || 14` で0はfalsy扱いで14に戻るが、負数や100以上は計算に反映される）。負数または61分以上でも結果が静かに表示され続ける | script.js の `computeWakeup`/`computeBedtime` 内でバリデーションを追加し、範囲外の場合は入力欄をエラースタイルにして「1〜60の数値を入力してください」のメッセージを表示 |
| style.css 全体 | ペルソナA（スマホ） | `input[type="number"]` にスピナー（上下矢印）が表示されるが、スマホでは操作しにくい。かつ `min`/`max` のHTML制約があっても手入力できる | CSS で `::-webkit-inner-spin-button` を非表示にし、ステッパーボタン（+/-）をカスタムで実装するか、`type="number"` の代わりに `type="text" inputmode="numeric"` を使用してより制御しやすくする |

---

### 3. 計算・結果の表示

**評価: ✅ 良好**

- 3件のカード（4/5/6サイクル）をページ読み込み直後から表示。スクロール不要でファーストビューに収まっている
- 「おすすめ」バッジ（5サイクル=7.5時間）がブルーで強調され、推奨が明確
- 大きいフォント（1.6rem→1.8rem）で時刻が見やすい
- `aria-live="polite"` でスクリーンリーダー対応済み

**改善提案（軽微）**:

| 場所 | ペルソナ | 問題 | 修正案 |
|------|---------|------|-------|
| script.js 97-109行目（起床モード） | ペルソナA | 起床モードで表示される就寝候補が「6サイクル → 4サイクル」の降順。ユーザーは「今日できる最短の候補」より「おすすめから見たい」はず | おすすめ（5サイクル）を先頭に、次に6・4の順か、または5→6→4の順（理想→余裕→ギリギリ）で表示すると直感的 |
| script.js 159行目 / script.js 215行目 | ペルソナB | 就寝カードの「card-time」には就寝時刻のみが大きく表示されているが、翌日0時をまたぐ場合（例: 22:14が前日就寝なのか不明）に混乱する可能性がある | 夜0時をまたぐ場合に `(翌日)` `(今日)` のような補足ラベルを添える |
| script.js 66-83行目 buildStageDots() | ペルソナC | ステージドット（ノンREM/REMの灰色・紫の点）がページ上に表示されるが、凡例がない。初見ユーザーには何を意味するか不明 | ドット直下か隣に `● ノンREM  ● REM` の簡易凡例を1回だけ表示するか、ツールチップ（title属性は実装済みだがモバイルでは機能しない）を補完する |

---

### 4. エラー・空状態

**評価: ⚠️ 要改善**

- `computeWakeup` / `computeBedtime` は `wakeupStr` が空のときに早期return（script.js 250行目）するが、ユーザーへのフィードバックが無い
- `type="time"` のブラウザ実装によっては手動でフォームをクリアできる場合があり、その際に結果エリアが「前回の結果を表示したまま」になる
- 入眠所要時間に不正値（0、負数、61以上）を入れても計算が続行される（前述）

| 場所 | ペルソナ | 問題 | 修正案 |
|------|---------|------|-------|
| script.js 250行目 `if (!wakeupStr) return;` | ペルソナB | 時刻入力が空の場合に結果が更新されず、古い結果が残る | 空状態の場合は結果エリアに「時刻を入力してください」などのプレースホルダーメッセージを表示する |
| script.js 249行目 `parseInt(wakeupLatencyInput.value, 10) \|\| 14` | ペルソナA | 0を入力すると14に戻る（0はfalsyのため）。ユーザーが意図的に0を入れた場合でも無言で14に差し替えられる | `0` も含むバリデーション `(val >= 1 && val <= 60) ? val : 14` に変更し、範囲外の場合は入力欄を赤枠にして理由を説明する |

---

### 5. モバイル・タッチ対応

**評価: ✅ 良好**

- `max-scale=1.0` でiOSの自動ズーム防止（index.html 13行目）
- `input[type="time"/"number"]` の `font-size: 1rem` でiOSのフォーカス時ズーム防止（style.css 133行目）
- `.mode-tab` の `min-height: 44px`（style.css 79行目）でタッチターゲット準拠
- `input` の `min-height: 44px`（style.css 139行目）でタッチターゲット準拠
- `@media (min-width: 480px)` でフォームが横並びになるレスポンシブ対応

**改善提案（軽微）**:

| 場所 | ペルソナ | 問題 | 修正案 |
|------|---------|------|-------|
| style.css 353-361行目 `@media (min-width: 480px)` | ペルソナA（iPhone SE等） | 480px以上でform-rowが横並びになるが、時刻inputと数値inputのサイズが均等（flex:1）になる。時刻inputは広く、入眠時間inputは狭くて良い | 数値inputのform-groupに`max-width: 100px`程度を設定し、視覚的なバランスを改善 |
| index.html 全体 | ペルソナA | 「今すぐ寝た場合」ワンクリック計算ボタン（SEO_REPORTの推奨機能7番）が未実装。就寝前スマホ利用の典型的ユースケースに対応できていない | 就寝モードのパネルに「今の時刻で計算」ボタンを追加し、現在時刻を自動セット |

---

### 6. 信頼性・プライバシー

**評価: ✅ 良好**

- index.html 117-119行目にlocalStorage保存の説明あり（外部送信なし明示）
- footerにプライバシーポリシー・問い合わせへのリンクあり
- titleに「無料」「登録不要」の記載あり（meta description 14行目）
- howto-sectionの説明コンテンツ（REM睡眠の根拠・米国睡眠財団引用）で科学的信頼感あり

**改善提案（軽微）**:

| 場所 | ペルソナ | 問題 | 修正案 |
|------|---------|------|-------|
| index.html 102-108行目 | ペルソナC | 「90分サイクル」「7.5時間おすすめ」の根拠として「米国睡眠財団」が挙げられているが（113行目）、脚注や参照リンクがない | `<small>※米国睡眠財団 (National Sleep Foundation) の推奨値に基づく</small>` などの出典表記を追加し、専門性・信頼性を強化 |

---

### 7. コンテンツ・離脱防止

**評価: ⚠️ 軽微な問題あり**

- howto-sectionに「使い方」「REM睡眠と90分サイクルとは」「睡眠時間の目安」「プライバシーについて」の4セクションがあり、SEO的にコンテンツ量は十分
- ただし、SEO_REPORT推奨の「FAQ（何時に寝ればいい？）」が未実装

| 場所 | ペルソナ | 問題 | 修正案 |
|------|---------|------|-------|
| index.html 89-126行目 howto-section | ペルソナB | 「何時に寝ればいい」「社会人の睡眠時間」などの検索意図に直接答えるFAQセクションが未実装。月間8,000〜12,000の口語KWを拾えていない | h2「よくある質問（FAQ）」セクションを追加。「何時に寝ればいい？」「子育て中のお母さんの睡眠は？」「6時間睡眠は体に悪い？」などQ&A形式でコンテンツを追加 |
| index.html 結果表示エリア（65行目/81行目） | ペルソナA/B | 計算結果を見た後に「他のツールも試す」「サイトトップへ戻る」などの次アクション誘導がない | 結果カードの下に「他のツールも試してみる →」などのCTAリンクを設置し、サイト内回遊率を向上 |
| index.html 104行目 `.howto-section h2:first-child` | ペルソナA | howto-sectionのh2「使い方」はページ下部にあるが、ファーストビューのdescと重複していて読まれにくい | 「使い方」セクションをページ上部（ツールパネル内）の短い文で完結させ、howtoはREM解説・FAQ中心に再構成 |

---

### 8. 広告・アフィリエイトの信頼性

**評価: ⚠️ 要改善**

- 広告ユニット（`#ad-top`・`#ad-result`）は現在空div（AdSense未有効化）のため実害なし
- **問題はアフィリエイトブロック（index.html 121-125行目）**

| 場所 | ペルソナ | 問題 | 修正案 |
|------|---------|------|-------|
| index.html 121-125行目 `.affiliate-block` | 全ペルソナ | **睡眠改善ツールなのに「ドメイン取るならお名前.com」という全く無関係なアフィリエイト広告が表示されている。**ユーザーは「このサイトは手抜きだ」と感じ、信頼感・滞在時間・再訪率が大幅に低下する。SEO_REPORTにも「睡眠サプリ・マットレス案件」の実装が推奨されている | **至急、睡眠関連のアフィリエイト案件に差し替える。**A8.netで「グリシン」「睡眠サプリ」「マットレス」などを検索し、承認されている案件のバナーまたはテキストリンクに変更。暫定的に案件が見つからない場合は、アフィリエイトブロック自体を削除するか「睡眠の質を高めるおすすめアイテムは準備中です」とプレースホルダー表示にする |
| index.html 122行目 `.affiliate-label` | ペルソナA | 「PR」ラベルの文字サイズが `0.7rem`（約11px）で非常に小さく、景品表示法の広告表示要件として視認性が不十分な可能性がある | 「PR」「広告」ラベルのフォントサイズを最低 `0.8rem` 以上に拡大し、色コントラストを `#6b7280` 以上に調整する |
| style.css 304-308行目 `.affiliate-label` | ペルソナB | `.affiliate-label` が `display: inline-block` のみで、アフィリエイトブロック全体がツールの解説コンテンツと視覚的に区別されにくい | アフィリエイトブロックに `border-left: 3px solid #f59e0b` など目立つアクセントを加え、「コンテンツと広告の視覚的分離」を明確化する |

---

## 未実装機能の確認（SEO_REPORT との対照）

SEO_REPORTで推奨された機能7件の実装状況を確認:

| # | 機能 | 実装状況 | 優先度 |
|---|------|---------|-------|
| 1 | 起床時刻→逆算（4〜6サイクル表示） | ✅ 実装済み | - |
| 2 | 入眠時刻→順算（4〜6サイクル表示） | ✅ 実装済み | - |
| 3 | 入眠所要時間スライダー（5〜30分） | ⚠️ numberインプット（min=1,max=60）で実装。スライダーではない | 低 |
| 4 | 推奨サイクル（7.5時間=5サイクル）バッジ | ✅ 実装済み | - |
| 5 | localStorage保存（前回値復元） | ✅ 実装済み | - |
| 6 | 「レム睡眠とは」FAQ/解説セクション | ⚠️ 解説あるがFAQ形式でない・「何時に寝ればいい」KW未対応 | 中 |
| 7 | 「今すぐ寝た場合」ワンクリックボタン | ❌ 未実装 | 中 |

---

## 発見事項一覧（修正必要度順）

### 要修正（デプロイ前に対応推奨）

| ID | 場所 | ペルソナ | 問題 | 修正案 |
|----|------|---------|------|-------|
| U-01 | index.html 121-125行目 | 全ペルソナ | アフィリエイト案件が「お名前.com（ドメイン登録）」で睡眠ツールと完全無関係。信頼感・UXを大きく損なう | 睡眠サプリ・マットレス案件に差し替え、または削除 |
| U-02 | index.html 33行目 h1 | ペルソナA/B | メインKW「睡眠時間 計算」（月14,800）がh1に未使用。h1は「睡眠サイクル計算ツール」となっている | SEO_REPORT推奨に従いh1を修正 |
| U-03 | script.js 249行目・259行目 | ペルソナA | 入眠所要時間の0・負数・61以上でバリデーションエラーが未表示。不正値が計算に使用される | 範囲外入力時のエラー表示を実装 |

### 軽微な改善提案（デプロイ後の改善で可）

| ID | 場所 | ペルソナ | 問題 | 修正案 |
|----|------|---------|------|-------|
| U-04 | index.html 34行目 subtitle | ペルソナB | 口語KW「何時に寝ればいい」がファーストビューに未登場 | サブタイトルに自然に挿入 |
| U-05 | script.js 97行目 `calcBedtimesFromWakeup` | ペルソナA | 起床モードの結果が6→4サイクル降順表示。おすすめ(5)が2番目 | 5→6→4順に変更 |
| U-06 | script.js 159行目 `card-time` | ペルソナB | 日付をまたぐ就寝時刻に「(翌日)」補足なし | 0時またぎ時の補足ラベル追加 |
| U-07 | script.js 66-83行目 `buildStageDots()` | ペルソナC | ステージドットの凡例がなく意味不明 | 凡例テキストを1回追加 |
| U-08 | index.html howto-section | ペルソナB | 「何時に寝ればいい？」FAQ未実装 | FAQ h2セクション追加 |
| U-09 | index.html 結果エリア下 | ペルソナA/B | 計算後のサイト内回遊誘導なし | 「他のツールも試す」CTAリンク追加 |
| U-10 | index.html 全体 | ペルソナA | 「今すぐ寝た場合」ワンクリックボタン未実装 | 就寝モードに現在時刻セットボタン追加 |
| U-11 | index.html 113行目 | ペルソナC | 米国睡眠財団の根拠に出典リンクなし | 出典注釈を追加 |
| U-12 | style.css 353-361行目 | ペルソナA | 480px以上でform横並び時のサイズバランスが悪い | 数値inputに `max-width: 100px` 設定 |

---

## 総合判定

**⚠️ 要修正（デプロイ前に U-01〜U-03 を対応すること）**

### 判定理由

ツールのコア機能（REM睡眠サイクル計算・カード表示・おすすめバッジ・localStorage復元）は完成度が高く、競合と比較しても十分に差別化できている。モバイル対応・iOSズーム防止・タッチターゲットも適切に実装されており、基本的なUX品質は高い。

しかし、以下3点がデプロイ前の修正必須事項：

1. **U-01（最重要）**: 睡眠ツールに「ドメイン取るならお名前.com」のアフィリエイトは完全にミスマッチ。AdSense審査でも「コンテンツと無関係な広告」として不利に働く可能性がある
2. **U-02**: メインKW「睡眠時間 計算」がh1に含まれておらずSEO機会を逃している
3. **U-03**: 入眠所要時間の不正値バリデーションが未実装で、異常な計算結果がユーザーに表示される可能性がある

U-01〜U-03 の修正後、再度レビューなしで **✅ 承認** とする。

---

*Generated by ux-reviewer agent — 2026-05-18*
