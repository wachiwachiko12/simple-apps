# UXレビューレポート — タイムゾーン横断型ミーティングスケジューラー

**レビュー日**: 2026-05-26
**レビュアー**: ux-reviewer (automated)
**ペルソナ**: グローバル企業勤務の日本人社員。英語は読めるが、日本語UIを好む。スマートフォン（iOS）でも業務ツールを使う。World Time Buddy の存在は知っているが日本語版を探していた。

---

## 1. ファーストビュー ✅

**所見**: ヘッダーのh1とsubtitleで「複数都市の時差を一覧表示 — 全員の業務時間が重なる会議時間を自動提案」というコアバリューが3秒以内に伝わる。デフォルトで東京・ニューヨーク・ロンドンが設定済みのため、初期表示から即座にグリッドが表示される。初見ユーザーが「何ができるツールか」を理解するのに十分な情報密度。

**問題なし**

---

## 2. 入力UX ✅

**所見**:
- 日付ピッカーとドロップダウンにlabelタグが適切に紐付いている
- 「都市を選択...」プレースホルダーが選択前状態を明示
- Enterキーでも都市追加できる（キーボードユーザー対応）
- 最大6都市制限が設定済み（silentにスキップ — 上限に達したらtoast通知あると更に良いが現状でも機能的には問題なし）
- 都市タグに削除ボタンあり、`aria-label` で「○○を削除」と読み上げ対応

**問題なし**

---

## 3. 結果表示 ✅

**所見**:
- カラーコーディングされたグリッド（緑=業務時間、青=推奨、グレー=時間外）が視覚的に明確
- 凡例（legend）がグリッド直下に配置されており色の意味が即理解できる
- 現在時刻行がオレンジのoutlineでハイライトされる（本日選択時）
- サマリーセクションが「重複業務時間: X時間（都市 HH:00〜HH:00）」形式で各都市バッジ付き表示 — 一目で会議可能時間帯が把握できる
- 重複ゼロ時は暖色の警告ボックスで「組み合わせを変えてください」とガイド

**問題なし**

---

## 4. エラー処理 ✅

**所見**:
- 都市未追加状態: 「都市を追加してください。」の空状態メッセージ
- 都市1つのみ: 推奨セクションに「都市を2つ以上追加すると...」プレースホルダー
- 重複する都市の二重追加防止（`find`でチェック）
- 最大都市数超過時はsilentにスキップ
- 日付クリア時は本日日付にフォールバック (`|| getTodayString()`)
- 業務時間重複なしの場合: 「業務時間が重なる時間帯はありません」と具体的ガイダンス付き

**問題なし**

---

## 5. モバイル ⚠️ → 修正済み

**所見（修正前）**:
- `<meta name="viewport" content="..., maximum-scale=1.0">` — iOS zoom 防止 ✅
- グリッドに `overflow-x: auto; -webkit-overflow-scrolling: touch;` で横スクロール対応 ✅
- `.btn-add`: `min-height: 44px` ✅
- `.control-item input[type="date"]`, `select`: `min-height: 44px` ✅
- `.back-link`: `min-height: 44px; line-height: 44px` ✅
- **問題**: `.city-tag .remove-btn` の `min-height: 24px; min-width: 24px` — 44px未満でタッチターゲット不足 ⚠️

**修正適用**: `.remove-btn` の `min-height`/`min-width` を 44px に変更、`padding: 2px` を `padding: 0` に変更（サイズを確保しつつパディング調整）

**修正後**: 全インタラクティブ要素が 44px 以上 ✅

---

## 6. 信頼性 ✅

**所見**:
- フッターに「プライバシーポリシー」リンクあり (`/privacy-policy.html`)
- 「使い方」セクション（5ステップの手順付き）あり
- 「タイムゾーン 時差ツールとは」説明文あり（World Time Buddy代替であることを明示）
- 「夏時間（DST）について」技術的説明あり（`Intl.DateTimeFormat` API使用を明記 — 信頼性向上に貢献）
- HTTPS配信前提の canonical URL 設定済み

**問題なし**

---

## 7. コンテンツSEO ⚠️ → 修正済み

**所見（修正前）**:
- **title**: `タイムゾーン ミーティングスケジューラー | 無料・日本語対応`
  — SEO_REPORTの推奨 (`タイムゾーン会議スケジューラー — 複数都市の時差を横並び比較・会議時間を自動提案 | 計算ラボ`) と不一致 ⚠️
- **h1**: `タイムゾーン ミーティングスケジューラー`
  — SEO_REPORTの推奨 (`タイムゾーン横断ミーティングスケジューラー — 複数都市の時差を一覧比較`) と不一致 ⚠️
- **meta description**: 「登録不要・無料」止まりで「リモートワーク・海外会議の時間調整」文脈が欠落 ⚠️
- **og:title**: titleと不整合 ⚠️
- **og:image** / **twitter:image**: `/images/ogp-timezone-scheduler.png` に正しく設定済み ✅
- GA4タグ `G-DK2WQM49L6`: `<meta charset>` 直後に設置済み ✅

**修正適用**:
1. `<title>` → `タイムゾーン会議スケジューラー — 複数都市の時差を横並び比較・会議時間を自動提案 | 計算ラボ`
2. `<meta name="description">` → SEO_REPORT推奨文に更新
3. `<meta property="og:title">` → titleと統一
4. `<h1>` → `タイムゾーン横断ミーティングスケジューラー — 複数都市の時差を一覧比較`

**修正後**: 主要SEOタグがSEO_REPORT推奨キーワードと完全に整合 ✅

---

## 8. 広告配置 ✅

**所見**:
```css
.ad-unit {
  margin: 1.25rem 0;
}
```
- `margin: 1.25rem 0;` のみ — 正しい ✅
- `min-height` なし ✅
- `background` なし ✅
- AdSenseスクリプトはコメントアウト済み（審査前の適切な状態）✅

**問題なし**

---

## 修正一覧

| # | ファイル | 変更内容 |
|---|---------|---------|
| 1 | `style.css` | `.city-tag .remove-btn` の `min-height`/`min-width` を 24px → 44px に変更（タッチターゲット確保） |
| 2 | `index.html` | `<title>` をSEO_REPORT推奨タイトルに更新 |
| 3 | `index.html` | `<meta name="description">` をSEO_REPORT推奨文に更新 |
| 4 | `index.html` | `<meta property="og:title">` をtitleと統一 |
| 5 | `index.html` | `<h1>` をSEO_REPORT推奨h1に更新 |

---

## 総合判定

**✅ 承認**

全8軸でブロッカー・重大な要修正項目なし。発見した3点（モバイルタッチターゲット、SEOタイトル系、meta description）はすべて本レポート内で修正済み。アプリのコア機能（時差グリッド、推奨時間帯計算、DST自動対応）は正常に動作し、ターゲットユーザー（グローバル企業勤務の日本人ビジネスパーソン）のニーズに的確に応えるUIを提供している。デプロイ可能。
